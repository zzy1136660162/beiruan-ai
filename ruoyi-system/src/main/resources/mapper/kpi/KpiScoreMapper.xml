<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.kpi.mapper.KpiScoreMapper">
	<resultMap id="KpiScoreResult" type="com.ruoyi.kpi.domain.KpiScore">
		<id property="id" column="id"/>
		<result property="taskId" column="task_id"/>
		<result property="score" column="score"/>
		<result property="comment" column="comment"/>
		<result property="createBy" column="create_by"/>
		<result property="createTime" column="create_time"/>
		<result property="updateBy" column="update_by"/>
		<result property="updateTime" column="update_time"/>
	</resultMap>
	<sql id="columns">id, task_id, score, comment, create_by, create_time, update_by, update_time</sql>
	<select id="selectKpiScoreById" parameterType="long" resultMap="KpiScoreResult">select <include refid="columns"/> from kpi_score where id=#{id}</select>
	<select id="selectKpiScoreList" parameterType="com.ruoyi.kpi.domain.KpiScore" resultMap="KpiScoreResult">
		select <include refid="columns"/> from kpi_score
		<where>
			<if test="taskId!=null"> and task_id=#{taskId}</if>
		</where>
		order by id desc
	</select>
	<insert id="insertKpiScore" parameterType="com.ruoyi.kpi.domain.KpiScore" useGeneratedKeys="true" keyProperty="id">
		insert into kpi_score(task_id, score, comment, create_by, create_time)
		values(#{taskId}, #{score}, #{comment}, #{createBy}, now())
	</insert>
	<update id="updateKpiScore" parameterType="com.ruoyi.kpi.domain.KpiScore">
		update kpi_score
		<set>
			<if test="score!=null"> score=#{score},</if>
			<if test="comment!=null"> comment=#{comment},</if>
			update_by=#{updateBy}, update_time=now()
		</set>
		where id=#{id}
	</update>
	<delete id="deleteKpiScoreById" parameterType="long">delete from kpi_score where id=#{id}</delete>
	<delete id="deleteKpiScoreByIds" parameterType="long">delete from kpi_score where id in <foreach collection="array" item="id" open="(" separator="," close=")">#{id}</foreach></delete>
</mapper> 